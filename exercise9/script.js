"use strict";

function bake(degrees) {
    let message;
    if (degrees > 250) {
        message = "I'm not a nuclear reactor!!!";
    }
    else if (degrees < 20) {
        message = "I'm not a fridge!!!";
    }
    else {
        message = "That's the right temperature for me :)";
        // setMode("baking");
        // setTemp(degrees);
    }
    return massage;
}

let status = bake(200);

